<UserControl x:Class="PALMS.WPFClient.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:PALMS.ViewModels;assembly=PALMS.ViewModels"
             d:DesignHeight="300" d:DesignWidth="100"
             d:DataContext="{d:DesignInstance viewModels:MenuViewModel}">

    <Menu DockPanel.Dock="Top">
        <MenuItem Header="_File">
            <MenuItem Header="_Client" Command="{Binding ClientCommand}"/>
            <MenuItem Header="_Linen" Command="{Binding LinenCommand}"/>
            <MenuItem Header="_Conveyor" Command="{Binding ConveyorCommand}"/>
            <Separator />
            <MenuItem Header="_Exit" Command="{Binding ExitCommand}"/>
        </MenuItem>

    </Menu>
</UserControl>
